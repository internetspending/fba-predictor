# Minimal env for local dev + CI (M2)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/fba_local

# Later milestones (keep commented for PR CI)
# KEEPA_API_KEY=
# AWS_REGION=
# AUTH_PROVIDER_SECRET=
