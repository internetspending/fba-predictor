[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = ["alembic"]
lint.select = ["E","F","I","UP","B","SIM","ARG","PL","RUF"]
lint.ignore = ["E501"]
# Ruff will prefer ruff.toml if it exists, but keeping pyproject.toml for backwards compat
format.quote-style = "double"
format.indent-style = "space"

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
strict_optional = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_unused_configs = true
disallow_untyped_defs = true
exclude = ["alembic", "apps"]

[tool.coverage.run]
branch = true
source = ["apps/api/app"]
